$(document).ready(function(){
   var names = $(".className").toArray();
   names.sort(function(a,b){
       return $(a).innerHTML.localCompare($(b).innerHTML)
   })
   for(var i = 0 ; i < names.length; i++){
    console.log(names[i].innerHTML);
   }

});

